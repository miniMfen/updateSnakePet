# SnakePet v5 更新日志

## v5.2.0-dev · OPT08 头饰佩戴修正（皇冠/圣诞帽/夜帽）（2026-09-20 过渡版本）

> 本条在分支 `feat/hat-fit-20260920` 上进行（用户要求，避免与另一会话在 master 上冲突）。

### 修复
- **圣诞帽歪戴（帽沿斜切过脸、后脑空着）**：`base_offset_deg` 原为 **30**，但该贴图画稿
  **本身就是正立的**（帽沿水平、绒球居中），这 30° 是无依据的额外旋转 → 真机上帽子整体顺时针歪过去。
  改为 **0**。3 倍放大 A/B：修复后帽沿水平扣在头顶、脸部完全露出。
- **夜帽同类问题（程度较轻）**：`base_offset_deg` 原为 **35**，同样改为 **0**。
- **皇冠比其它头饰小、且戴得太低**：`scale` 原为 **0.82**（7 顶里最小，渲染高度仅约礼帽的 60%）；
  `anchor_y` 原为 **-0.88**（佩戴点偏低，显得卡在脸上）。改为 `scale=1.10` / `anchor_y=-1.02`。
- **佩戴线（pivot）从未生效（根因级）**：`load_registry()` 用 `merged.update(data)` 做**整条替换**，
  而 `sprites/hats/hats.json` 里 7 顶帽子**都没写 `pivot`** → `DEFAULT_REGISTRY` 中逐顶标定好的
  佩戴线（夜帽 `[0.58,0.92]` / 圣诞 `[0.5,0.94]` / 学士 `[0.5,0.62]` / 礼帽 `[0.5,0.88]` …）
  **被整条丢掉**，全部退回 `_auto_pivot` 自动检测 —— 精心校准过的佩戴线**从来没有生效过**。
  改为**逐字段合并**：json 里给出的字段覆盖默认值，未给出的字段保留默认。

### 测试
- 新增 `tests/test_hats_meta.py`（6 例）：json 覆盖默认字段 / **pivot 不被丢弃** /
  `hats.json` 与 `DEFAULT_REGISTRY` 逐字段一致 / **正立画稿的 `base_offset_deg` 必须为 0** /
  皇冠不得明显偏小偏低 / pivot 确实参与对齐计算。**新增而非修改**，未触碰 T09 锚点矩阵；pytest 81 → 87。

### 工程
- `DEFAULT_REGISTRY` 与 `sprites/hats/hats.json` 逐字段对齐（消除两份真源；此前
  `cap_blue`/`bowknot`/`tophat`/`graduation` 的锚点在两处本来就不一致）。
- G1/G2/G3 全 PASS；探针 19 场景 MATCHED。
- 真机需确认项：分别戴皇冠、圣诞帽、夜帽游走，帽子应正立扣在头顶、不挡眼、尺寸相称。

### 已知未修（待确认）
- 帽子锚点随朝向**整圈公转**：`hat_anchor_pos` 把锚点也按 heading 旋转，于是 heading≈180° 时
  帽子跑到头的**下方**且贴图倒转 180°，与「眼睛固定以屏幕上方为正」的画法不一致。
  修它要改动被单测（T09）覆盖的契约且影响全部 7 顶帽，属设计决策，本轮不动。

## v5.2.0-dev · OPT07 修复拖拽身体断层的真正根因（2026-09-20 过渡版本 · 回归修复）

> OPT04 修的是「限速层级」（一帧内多次调用），那是真实缺陷但**不是用户看到的断层**。
> `CHANGELOG` 该条目此前遗漏，本次一并补入。

### 修复
- **拖拽时身体断层（真正根因）**：`render._sample_body()` 处理「目标弧长越过当前段」时用的是
  **绝对容差**（`t>1 且 t<1.5 → 钳到段端点`）。该容差是按**安静档步距 `QUIET_STEP=1.6px`** 标定的
  （注释原文即「微小幅差（浮点噪声）钳到段端」）。而拎起时 `_drag_head_to` **每帧只落一个路径点**，
  段长 = `DRAG_MAX_STEP = 30px` → `d` 一进入某段 `t` 立刻 >1 → 采样点被钳到段端点，
  即**被"吸附回原始折点"** → 相邻绘制点距由 8px 退化为段长本身。

  实测（78 节长蛇，光标 400px/帧，量的是 `_sample_body` 实际输出）：

  | 场景 | 相邻绘制点距 max | 判定（断裂阈值 = 身直径 20px） |
  |---|---|---|
  | 正常游动 | 11.53 px | 无断裂 |
  | 慢速拖拽 | **35.26 px** | 几乎每帧都断 |
  | 快甩（1 事件/帧） | **30.00 px** | 几乎每帧都断 |
  | 快甩（20 事件/帧） | **30.00 px** | 几乎每帧都断 |

  修复后四种场景**一律 8.00 px**（= 采样步长本身），零断裂帧。
  修复方式：越界时不再钳制，而是**前进到真正包含 `d` 的那一段再插值** —— 与段长无关。
  同时把采样循环的 guard 上限由固定 5000 改为按（路径点数 + 采样点数）推导，防长蛇截断。

### 测试
- 新增 `tests/test_render_sample.py`（10 例）：把「相邻绘制点距 == 采样步长」钉成不变量，
  参数化覆盖段长 1.6 / 8 / 20 / **30（DRAG_MAX_STEP）** / 50 px 与直线、曲线两种拓扑。
  **新增而非修改**；pytest 71 → 81。

### 方法学纠正
- OPT04 的验证脚本量的是 `app.seg_pos` —— 那是 `snake.pos()` 的 20px 粗采样，供互动命中与帽子锚点
  使用，**不是画面上的身体**。画面身体来自 `_draw_snake()` 的 `_build_render_path()` + `_sample_body()`，
  是**另一条数据通路**。故「423.79px → 30.00px」对画面不成立。
  已改用渲染级复现器（出帧图 + 量 `_sample_body` 实际点距）。

## v5.2.0-dev · OPT06 修复菜单「退出」键消失（2026-09-20 过渡版本 · 回归修复）

### 修复
- **菜单里「退出」键消失**（OPT05 引入的回归）：`MENU_PH` 的面板高度公式里硬编码着
  「开关×4」,OPT05 把开关组加到 5 个之后没有同步,面板因此少算一行(38px),
  最后一行「退出」被画出画布 —— 真机上只露出 12px,被圆角边框压住看不见。
  实测:面板高 640 → 678;「退出」行 y 区间 628..666,修复前仅 628..640 可见(12px)。

### 变更(结构性加固,杜绝同类回归)
- 开关组提取为类常量 `TOGGLE_ITEMS`,作为**唯一真源**:高度公式与布局都从它推导,
  不再各写一份行数。
- 新增 `_menu_ph` 属性:渲染与命中判定改为以 `_menu_layout()` **实测**的 `_menu_h`
  为准,而不是预估公式 —— 即便公式再次漏同步,也只会告警,不会再裁掉行。
- `_menu_layout()` 结尾比对预估与实际,不一致时 `logging.warning`。

### 测试
- 新增 `tests/test_menu_layout.py`(7 例)—— `menu.py` 此前零单测(569 行)。
  覆盖:高度预估==实测、最后一行是且仅为「退出」、所有命中矩形落在面板内、
  开关行与 `TOGGLE_ITEMS` 一一对应、退出行回调真的调 `quit`、
  长度输入编辑器展开时同样装得下、渲染图像真的把最后一行画进去(像素级)。
  **新增而非修改**,未触碰任何既有断言;pytest 64 → 71。

## v5.2.0-dev · OPT05 新增「不再自然变短」开关（2026-09-20 过渡版本）

### 新增
- **菜单开关「不再自然变短」**:默认关闭。开启后,即使长时间不喂食(饱食度低于 40)
  小蛇也不会把身体缩回当前阶段的基础长度,体长保持不变。配置项 `no_digest`。
- 命名对齐既有风格(「不再吃食物」「左键点击不再生成食物」);`DEFAULT_CFG` 默认 `False`,
  保证不改变任何人已有的行为。

### 工程
- 消化逻辑整体置于开关之下,**未改动 `digest_step` 的既有签名**(它有单测覆盖)。
- 专项验证 5/5 PASS:关闭时 2000 帧缩 88.00px(与理论速率 `DIGEST_PX_PER_SEC×TICK_MS/1000×帧数`
  精确一致)、开启时缩 0.00px、配置写入后可读回、老配置缺字段自动取默认值、默认值确为 False。
- 菜单 14 行 → 15 行;探针「菜单」场景像素差异 29.115%,其余 19 场景 0.000%;
  菜单图尺寸(254×640)与 alpha 覆盖率均不变(无行被挤出或裁切),已目视核对对照图。
- pytest 64/64、`--selftest` OK。
- 真机需确认项:右键菜单开关组应出现第 3 行「不再自然变短」,点击可切换且重启后保持。

## v5.2.0-dev · OPT04 拖拽身体断层修复（2026-09-20 过渡版本）

### 修复
- **鼠标拖拽时身体断层**:`DRAG_MAX_STEP` 的本意是「**每帧**头部位移上限」,但 `_drag_head_to`
  对每一个 `'drag'` 事件都被调用一次,而高回报率鼠标一帧可投递数十个 MOVE 事件 →
  一帧内连续调用数十次,限速形同虚设。实测(83 节长蛇、光标 400px/帧):

  | MOVE 事件/帧 | 头部单帧位移 |
  |---|---|
  | 1 | 30.00 px(正常) |
  | **20** | **423.79 px(限速失效)** |

  一帧跳 400+ px 会把身体拉成一条横跨数百像素的直线,肉眼即「身体断层」。
  修复:引入帧序号,`_drag_head_to` 每帧只推进一次;因该函数内部一律取
  `self._interact.cursor`(本帧已被全部 MOVE 更新到最新),只执行第一次不会丢精度。
  修复后 20 事件/帧 的头部单帧位移 = **30.00 px**。

### 工程
- 新增专项验证器(工作流 `runs/.../impl/draggap_check.py`):4 模式对照 ——
  正常游动 / 慢拖 / 快甩(1 事件/帧) / 快甩(20 事件/帧),逐帧测量**实际绘制出的**身体分段间距。
- pytest 64/64、`--selftest` OK;探针 20 场景 0.000%(拖拽不影响静态探针,符合预期)。
- 顺带发现(未修,记为待办):正常游动时身体分段间距为 **24px** 而非 `SEG=20px`。
  球半径 10px 时 24px 仍重叠 16px,肉眼不断开,故不阻塞本轮。

## v5.2.0-dev · OPT02 皮肤语义调整（2026-09-20 过渡版本）

### 变更
- **默认皮肤改为「经典绿」、翡翠改为需解锁**:按用户指示采用**只互换显示名**的做法 ——
  两套配色的数值与索引顺序完全不动,按下这两个按钮得到的颜色与改动前一致(用户明确要求
  「按下这两个按键后的颜色跟原来是一样的」)。名/色的对应关系已在 `constants.py` 注释中显式记录,
  日后若要严格对齐只需换回两行。
- **皮肤解锁门槛大幅降低**:`SKIN_UNLOCK_EATEN` 由 `(0,10,30,60,100)` 改为 `(0,3,8,15,25)`。
  当前累计进食 8 颗,改动后可解锁到第 3 套(蜜桃粉)。
- `config.py` 迁移链的默认皮肤由硬编码 `'jade'` 改为引用 `DEFAULT_THEME`,消除第二处默认值真源
  (原先只改 constants 会导致新装用户与老用户升级后拿到不同默认皮肤)。

### 工程
- 授权契约变更 **CONTRACT-CHANGE-01**:`tests/test_growth.py` 的 5 处门槛边界与解锁提示文案同步为新契约。
  修改发生在实现之前、覆盖不弱于旧断言、逐条记录于工作流 `runs/.../scope/contract_hits.json`。
- 渲染探针 20 场景中 **19 个像素差异 0.000%,仅「菜单」场景 0.260%**(皮肤 chip 文案互换所致) → G3 PASS。
- pytest 64/64 全绿、`--selftest` OK → G1 PASS。
- 真机需确认项:右键菜单皮肤行应显示「经典 翡翠 蜜桃 夜光 小丑」,且默认选中第一位「经典」。

## v5.2.0-dev · OPT01 睡觉流程重做（2026-09-20 过渡版本）

> 本条目为 v5.2.0 的第一段过渡版本,后续 OPT02(皮肤)与 OPT03(窗口 z 序)完成后合并为正式 v5.2.0。

### 新增
- **睡觉前先盘旋**:饱食度归零后不再就地入睡,而是进入「盘旋准备」阶段,完成一次分层盘旋后才开始打呼噜。
  盘不起来时重试 5 次,仍失败则降级为直接入睡(记日志,不空转)。
- **睡眠时长 6~12 秒随机**:且计时起点改为**盘旋结束**那一刻(原为 15~25 秒、从入睡瞬间计时)。
- **唤醒冷却 5~8 分钟随机**:醒来后在一段时间内不会再想睡,解决「睡觉太频繁」。
- 新增常量 `SLEEP_DUR_RANGE` / `SLEEP_COIL_ATTEMPTS` / `SLEEP_COIL_RETRY_MS` / `SLEEP_COOLDOWN_RANGE`。

### 修复
- **盘旋功能在带全屏透明覆盖层的桌面上完全失效**(AMD-01):Windows 11 的 `ShellHandwritingCanvas`
  (手写墨迹输入层)是「全屏 + 置顶 + 分层」窗口,被 `_refresh_avoid` 当作障碍物并向外扩 240px 后
  覆盖了整个工作区,使 `make_coil_plan()` 恒返回 `None` —— **v5 招牌的分层盘旋在这些机器上从未生效**。
  实测:修复前可用盘旋中心 0/2240(0.0%),修复后起盘成功率 5/5。
  这也是「睡觉前先盘旋」的前置条件。新增 `FULLSCREEN_OVERLAY_COVER_RATIO` / `SHELL_OVERLAY_CLASSES`。
  注:**渲染探针抓不到这个缺陷** —— 探针是 headless,`_refresh_avoid` 首行直接 return,`_avoid` 恒为空。

### 变更
- `_mood()` 语义细化:饱食度归零但尚未入睡(含盘旋准备期)显示为「饿」而非「睡」,不再提前闭眼;
  真正「睡」由新增的 `_is_dozing()`(复用既有 `_sleep_since` 语义)判定。
- `_is_sleeping()` 保持 `satiety <= 0` 语义不变,既有测试与渲染探针无需改动。

### 工程
- 睡觉流程改为三阶段状态机(`_sleep_step` / `_begin_doze` / `_end_doze`),主循环 `_step_impl`
  仅在真正打呼噜时跳过运动,盘旋准备期走正常运动分支以保证盘旋可推进。
- 渲染探针 20 场景像素差异全为 `0.000%`,零视觉回归(G3 PASS)。
- pytest 64/64 全绿,`--selftest` OK,测试文件**未做任何改动**(G1 PASS)。



## v5.1.2 (2026-09-19 第三轮体验反馈修复)

### 新增
- **蛇身长度手动输入**:点击菜单「蛇身长度」的节数数字,弹出屏幕数字键盘(1~0/退格/清空/确定/取消),直接输入目标长度,自动钳制在 2~100 节。

### 修复
- **皇冠遮挡蛇脸**:皇冠缩小(1.08→0.82 倍)并上移到头顶,不再当"头盔"盖住眼睛。

### 优化
- **帧率与画质自适应**:渲染像素预算随实测帧耗时自动升降(快时自动提高细腻度、慢时自动降档保帧率),实测平均 38.9 FPS;帧缩放改用 BILINEAR,更快且超采样下视觉无损。

## v5.1.1 (2026-09-19 第二轮体验反馈修复)

### 修复
- **身体细小扭动(不平整路面感)**:渲染路径新增滑窗平滑——按弧长 ±26px 对路径顶点滑动平均,小转角折点被圆弧化,身体曲线如参考图般干净丝滑;腹带由圆点链改为带 joint 的连续描线,消除波浪边。
- **头身脱节**:修复三处叠加缺陷——①圆弧替换会跳过路径末尾原始头点;②平滑端部渐缩逻辑写反(头部被平滑拉离原位);③补长段被边界钳制后 dist 与 XY 弧长不一致导致身体采样为空/错位。现在头身永远衔接。
- **夜帽/圣诞帽佩戴悬空**:帽子佩戴点改为从贴图 alpha 自动检测(下部最宽行的中心=帽沿),不再依赖手工估计;夜帽/圣诞帽/皇冠/学士等各顶均贴合头顶。

### 优化
- **转弯弧度加大**:转向速率减半(安静档 0.10→0.045、活跃档 0.18→0.095 rad/帧),转弯半径 = 步速/转速,活跃档约 100px 大弧,与参考图弯曲形态一致;避让探测距离随转弯半径匹配(170px 多点采样),障碍绕行同样圆滑。

## v5.1.0 (2026-09-19 首轮体验反馈修复)

### 修复
- **帽子定位重做**:不再整体居中贴图,改为「佩戴线」锚定(贴图内指定佩戴点精确对齐头顶);夜帽/圣诞帽/皇冠逐顶校准,不再遮挡蛇脸。
- **菜单帽子/皮肤/成就行的存档污染**:soak 与渲染探针曾与用户真实宠物共用存档,已强制隔离(reports/_tool_home),测试不再写入用户数据。

### 新增
- **最小转弯角度**(2.9° 死区):低于阈值的微碎转向不再发生,游动转弯更圆润自然。
- **「记住小蛇」开关**(默认开启):自动存档可一键关闭,关闭后每次开机都是新小蛇。
- **初始体长加长**:5 节 → 8 节。

### 优化
- **帧率提升约 1.5~2 倍**:目标帧率 33ms → 22ms(约 45fps),实测平均 41.8 FPS(此前 20~28)。
- **画质**:超采样改为像素预算自适应(渲染像素恒定 ≤110 万)——小画布 2× 抗锯齿去马赛克,大画布自动降档保帧率。

## v5.0.0 (2026-09-19)

从 SnakePet v4 的整体升级,按 P0~P7 流水线完成。

### 新增
- **360° 自由游动**(F1):连续角度转向器替代四方向格点移动;闲逛高斯漂移 + 趋势角重置 + 边界内推场 + 避让扇形采样;追食 atan2 目标角 + 卡死偏置。(P1)
- **分层盘旋**(F2):盘入→盘踞→盘出三阶段阿基米德螺线,身体形成多层同心圈;空闲触发与菜单「盘旋一次」;食物/拎起快速盘出中断;短蛇自动降级小圈。(P2)
- **蛇形象优化**(F3):以 Microsoft Fluent 3D 蛇为基准重绘——七层绘制(投影/外轮廓/渐变锥形主体/腹带/菱形斑/部件化头/帽)、吐信动画、鼻孔、固定高光大眼;snake_head.png 整头贴图覆盖通道。(P3)
- **帽子衣柜**(F4):7 顶帽子(夜帽/圣诞/皇冠/学士/礼帽/棒球/蝴蝶结)随朝向旋转;hats.json 锚点元数据;缺图内置手绘兜底;auto 档保留夜间夜帽语义。(P4)
- **互动增强**(F5):投喂(菜单按钮)、抚摸(按住头部 0.3s,冷却 2s)、拎起拖动(press/hold/drag 状态机);与撒食/菜单/双击等既有交互零冲突。(P5)
- **养成系统**(F6):消化回收体长、幼蛇→成蛇→大蛇三阶段(平滑过渡)、胖瘦体型、5 套皮肤解锁(累计进食 10/30/60/100)、8 个成就、pet_state.json 完整存档(退出/60s 周期/关键事件即时保存,坏档自动备份)。(P6)

### 变更
- 配色主题化:默认「翡翠玉蛇」(Fluent 3D 量化取色),经典绿(v4 原配色)等共 5 套皮肤可切换。
- 菜单扩容:帽子两行网格、皮肤、成就、投喂、盘旋等行;开关滑块视觉补全。
- 蛇身双击语义:单按蛇身改为无动作(防误触),跳跃移至双击(任务书冲突矩阵)。
- 单实例互斥名改为 `SnakePet_v5_Mutex_V1`,可与 v4 原版并存。

### 修复
- 重建自 v4 字节码仲裁版,修复 6 处反编译残留损坏(auto 档速度条件、空闲动作计时、避让窗口枚举、睡帽菜单赋值、开关 toggle 逻辑、蛇贴图取色块)。
- 修复 `Snake.pos()` 潜藏插值段错误(拎起大步距下暴露,误差达 14px)。
- 修复抚摸无法由静止持住触发的问题。

### 工程
- 模块化包结构(10 模块)、CLI 三件套(--selftest/--soak/--render-probe)、pytest 61 例、
  全阶段 Gate 证据(报告/浸泡 JSON/渲染探针)随仓库 `reports/` 交付。

### 素材致谢
- Snake/Apple/Crown/Top hat/Billed cap/Graduation cap/Ribbon 3D:© Microsoft Fluent Emoji(MIT License)
- Santa hat:いらすとや(https://www.irasutoya.com/)(作品内使用)
- Noto/OpenMoji 素材仅作参考,未随包分发。
